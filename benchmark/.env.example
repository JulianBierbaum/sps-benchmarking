# PLC IP Address
IP=192.168.106.62

# WebAPI Authentication
WEBAPI_USER=5AHIT
WEBAPI_PASSWORD=5ahiT2025

# OPC UA Configuration
OPCUA_PORT=4840
OPCUA_NAMESPACE=3

# Variable paths for OPC UA (these match the WebAPI variable paths by default)
OPCUA_SINGLE_VAR='"PerformaceData".ToServer.bool00'
OPCUA_BULK_VAR='"PerformaceData".PlcData.BulkData'

# S7 Protocol Configuration (Matched to Projekt5AHIT_SYT.pdf)
S7_RACK=0
S7_SLOT=1

# DB7 "Performace DataS7" 
S7_DB_NUMBER=7 

# bool00 is at Offset 0.0 
S7_BOOL_OFFSET=0 

# int16_01 is at Offset 2.0
S7_INT16_OFFSET=2

# int32_01 is at Offset 12.0
S7_INT32_OFFSET=12

# BulkData starts at Offset 136.0 
# (Previous variables occupy bytes 0-135)
S7_BULK_OFFSET=136 

# LTime is 64-bit (8 bytes)
S7_BULK_ELEMENT_SIZE=8

# Benchmark Configuration

# Test duration for single write benchmarks (seconds)
BENCHMARK_DURATION_SECONDS=5

# Target rates for single write tests (comma-separated, ops/s)
BENCHMARK_TARGET_RATES=1,10,20

# Number of repetitions for bulk write test
BENCHMARK_BULK_REPETITIONS=10